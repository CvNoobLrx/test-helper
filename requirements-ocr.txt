# Optional OCR dependencies for scanned/photo PDF ingestion.
# Install after the base requirements:
#   python -m pip install -r requirements.txt -r requirements-ocr.txt

rapidocr>=3.0.0
onnxruntime>=1.14.1
